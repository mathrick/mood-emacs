(defflag -forge "Do not enable Magit's forge functionality,ie. support for Github, Gitlab, etc.")
(defflag +always-show-recent "Always show recent commits. By default, Magit only shows recent if there are no unpushed ones")
