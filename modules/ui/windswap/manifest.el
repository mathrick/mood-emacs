(defflag -follow-windmove "Following windmove means the modifier is added to the modifier used by windmove, otherwise it's standalone")
(defflag :modifier 'shift "Windswap prefix key, interpretation depends on the value of FOLLOW-WINDMOVE")
