"Tools and support packages that help with searching things
through grep, git grep, etc."

(defflag :grep-a-lot '(:keys t) "Whether to use grep-a-lot. nil to disable, otherwise a plist with
sub-options. :keys nil disables default key bindings")
